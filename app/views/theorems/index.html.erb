<p id="notice"><%= notice %></p>

<h1>Listing Theorems</h1>

<table>
  <thead>
    <tr>
      <th>Text</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @theorems.each do |theorem| %>
      <tr>
        <td><%= theorem.text %></td>
        <td><%= theorem.user_id %></td>
        <td><%= link_to 'Show', theorem %></td>
        <td><%= link_to 'Edit', edit_theorem_path(theorem) %></td>
        <td><%= link_to 'Destroy', theorem, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Theorem', new_theorem_path %>
